This folder is intentionally empty in the initial commit.

Running `npm test` (after `npx playwright install --with-deps`) generates
`html-report/` and `results.json` here. Commit the REAL output of a real run
— every prior attempt at this exercise was marked down for either missing
execution evidence or a committed report showing failures from browsers never
being installed. An empty or fabricated report here is worse than an honest
one showing real failures you're actively fixing.
